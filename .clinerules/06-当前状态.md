# 当前状态（截至 commit a3890c7）

## 已完成
- Git 仓库初始化 + 备份到 vxapo-driver-bak
- 旧代码清理 + checkpoint
- 目标树全声明补全 + 参与编译
- config 12 commands 补齐
- GUID 封装（StringFromGUID2 in prelude）
- SAM_READ 修复（0x0002_0019）
- DSP 工厂 15 槽位全接线（9 个 DSP 工厂实现，index 0-5 为纯配置命令）
- **v6.4 适配（commit cba65fd）**：
  - install/selector 拆分为 select.rs + operation.rs，selector.rs 为纯入口
  - enumerate_devices 真实枚举（Render + Capture，经 enum_sub_keys）
  - sys/registry.rs 补齐枚举/备份 API + get_guid_string + save_to_file（.reg 导出）
- **DSP 滤波器完成度（commit 7be0aab）**：
  - convolution.rs 短 IR 时域 FIR 卷积 + WAV 解析（PCM16/f32）
  - VST 定位为预留 NoMatch；FACTORY_COUNT=15 不变
  - 10 个滤波器 process() 全部真实实现，无空壳
- **object/apo.rs 骨架补全（commit a325ad3）**：
  - LockForProcess 完整链路 + APOProcess 双链过渡 + GetRegistrationProperties + IsFormatSupported + hot_reload
- **v6.9 适配（commit 0589669，tympan-apo/EAPO 借鉴）**：
  - **O1** RealtimeContext（零尺寸，contract.rs）+ DspContext::rt_marker（PhantomData，6 处测试构造已补）
  - **O2** StateCell 重写：TransitionError{expected,attempted,actual} + release() + initialize/lock/unlock 语义化
  - **O3** release `codegen-units=1`（panic=abort 已有）
  - **O4** `feature="aec"` 门控 3 个 AEC 接口 + IID 预留（apo_interfaces.rs）
  - **E1** Filter::is_in_place()（默认 true）+ Chain::is_fully_in_place()
  - **E3.2** DeviceInfo::is_disabled()/is_unplugged()（EndpointState 推导）
  - **E3.3** InstallConfig::auto_adjust 独立字段（默认 false），Step4 写注册表读取
  - **E3.4** install_endpoint(..., verify)：CoCreateInstance 自检，失败不自动回滚
  - **R1** retired_chain 退役链延迟析构（RT 过渡完成帧零析构，控制线程锁内统一 drop）
  - **R2** reloading 阻塞式重载（hot_reload 检查 transition||reloading；APOProcess 过渡完成触发）
  - **R3** Chain::is_empty() + process_audio 空链快路径（跳过链遍历）
  - **R4** default_smoothing_length 50ms→10ms（sample_rate/100）
- **P0-1 DllRegisterServer 补全（commit a3890c7，roadmap P0-1 实现+测试）**：
  - DllRegisterServer：DLL 路径获取失败 → SELFREG_E_CLASS；注册失败按逆序回滚已注册条目 → SELFREG_E_CLASS（Note 29）
  - DllUnregisterServer：先删 InprocServer32 子键再删 CLSID 父键，键不存在视为成功（幂等，Note 30）
  - register_com_class/unregister_com_class 改用 sys/registry（RegKey::create + write_sz / delete_tree），失败透出具体 HRESULT
  - 本地常量 SELFREG_E_CLASS = 0x80040201（windows crate 未导出）
  - roadmap.md P0-1 已追加"实现完成报告"（DoD ☑实现 ☑测试，不动状态，待规范侧核对 Done）
- cargo check 0 errors / 0 warnings
- cargo test 417 passed / 0 failed

## 进行中 / 待做
1. P0 系列（roadmap Spec-Finalized）：
   - P0-2 config.txt 解析链路补齐（parser/commands/factory 实现缺口）
   - P0-3 per-device 配置路径（v7.2，依赖 P0-1/P0-2）
   - P0-4 配置热重载全链路（v7.3，依赖 P0-3）
   - P0-1 待规范侧完成/退回（手动验收 regsvr32/CoCreateInstance 待真实环境）
2. clippy 集中收尾（冻结中，待功能骨架定型）
   - 补 SAFETY 注释（COM/FFI 几十处 unsafe）
   - 修单向 lint（loop/Default/let/redundant closure/transmute 等）
3. pipeline 核心模块完成度（context/buffer/format/chain/process/interleave——已确认签名可用，待统一验证）
4. object 子模块剩余（child/factory/ref_count/dll_exports 待对照 v6.4 规范核实）
5. telemetry 模块完成度
6. config/watcher.rs → object/apo.rs::hot_reload 的接线（watcher 触发链路待确认）

## 已解决疑问
- DSP 滤波器 process() 完成度验证完成：10 个全部真实实现；VST 预留 NoMatch
- apo.rs 骨架 4 处占位全部补齐
- v6.6-v6.9 changelog 已通读并适配（O1/E1/R1-R4/O2/O3/O4/E3.2-E3.4）

## 未解疑问清单
- parser 的命令分发链路：命令关键字 vs DSP 类型关键字在 parse_lines_impl 中的具体区分机制
- install/audiodg.rs 与 selector/operation.rs 的 DisableProtectedAudioDG 检查时机（ensure_can_load 已在 LockForProcess 末尾调用）
- config/commands/*.rs 的 handle 函数目前是否被 parser 实际调用（还是只通过 registry.try_create）
- 6 个未注册的 index 0-5 工厂（纯配置命令）是否需要在 FilterRegistry 中占位
- config/watcher.rs 检测到配置变化后如何触发 apo.rs::hot_reload（需要 watcher → 持有 ApoObject 的关联；当前 hot_reload 是 pub 方法待接线）
- utils/guid 冲突已澄清：GUID→String 统一走 prelude；bytes→GUID 就地内联