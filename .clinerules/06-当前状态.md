# 当前状态（截至 commit 11a381f）

## 已完成
- Git 仓库初始化 + 备份到 vxapo-driver-bak
- 旧代码清理 + checkpoint
- 目标树全声明补全 + 参与编译
- config 12 commands 补齐
- GUID 封装（StringFromGUID2 in prelude）
- SAM_READ 修复（0x0002_0019）
- DSP 工厂 15 槽位全接线（9 个 DSP 工厂实现，index 0-5 为纯配置命令）
- **v6.4 适配（commit cba65fd）**：
  - install/selector 拆分为 select.rs + operation.rs，selector.rs 为纯入口
  - enumerate_devices 真实枚举（Render + Capture，经 enum_sub_keys）
  - sys/registry.rs 补齐枚举/备份 API + get_guid_string + save_to_file（.reg 导出）
- **DSP 滤波器完成度（commit 7be0aab）**：
  - convolution.rs 短 IR 时域 FIR 卷积 + WAV 解析（PCM16/f32）
  - VST 定位为预留 NoMatch；FACTORY_COUNT=15 不变
  - 10 个滤波器 process() 全部真实实现，无空壳
- **object/apo.rs 骨架补全（commit a325ad3）**：
  - LockForProcess 完整链路 + APOProcess 双链过渡 + GetRegistrationProperties + IsFormatSupported + hot_reload
- **v6.9 适配（commit 0589669，tympan-apo/EAPO 借鉴）**：O1/O2/O3/O4/E1/E3.2-E3.4/R1-R4 全部落地
- **P0-1 DllRegisterServer 补全（commit a3890c7，roadmap P0-1 实现+测试）**：
  - DllRegisterServer/DllUnregisterServer 完整流程 + SELFREG_E_CLASS + 逆序回滚 + 幂等注销
  - register_com_class/unregister_com_class 改经 sys/registry，失败透出具体 HRESULT
  - roadmap.md P0-1 已追加"实现完成报告"（DoD ☑实现 ☑测试，不动状态）
- **P0-2 config.txt 解析链路补齐（commit 11a381f，roadmap P0-2 实现+测试）**：
  - config/parser.rs 重写：ConfigParser 三入口 → parse_content/parse_lines_impl 逐行分发（规范 6.1）
    - 条件分支（If/ElseIf/Else/EndIf）始终处理 + false 分支跳过 + 条件栈平衡检查
    - 纯配置命令（Device/Stage/Channel/Eval/Include/Filter/GraphicEQ/Preamp/Copy/Delay）分发各 handle
    - REW `Filter N:` 动态命令名分发 rew::handle（逗号小数规范化）
    - 其余经 registry.try_create（裸 IIR/Biquad/Convolution/LoudnessCorrection）+ Unmatched log::warn
    - AbortFile 语义：终止当前文件（含 Include 子文件），不传播错误、不报致命错误
    - read_config_file：UTF-8 优先 + BOM 跳过 + 非 UTF-8 lossy 降级
    - ParseContext.current_file 由 &'a Path → PathBuf（消除 Include 递归 Box::leak 泄漏）
  - include.rs：去 Box::leak 路径泄漏，深度限制 16 层保留
  - filter.rs / rew.rs：OFF → PassthroughFilter（规范 6.14 语义，链序号保留）
  - 测试 417 → 436 passed（新增 parser 逐行分发/集成样例/Include 递归/AbortFile/条件/Eval/Stage/REW/OFF/ANSI lossy/BOM）
  - cargo check 0 errors / 0 warnings
  - roadmap.md P0-2 已追加"实现完成报告"（DoD ☑实现 ☑测试）+ "反馈"段（3 点规范澄清建议，未动状态）
- cargo check 0 errors / 0 warnings
- cargo test 436 passed / 0 failed

## 进行中 / 待做
1. P0 系列（roadmap Spec-Finalized）：
   - P0-3 per-device 配置路径（v7.2，依赖 P0-1/P0-2 已 Spec-Finalized ✅）← **下一个任务**
   - P0-4 配置热重载全链路（v7.3，依赖 P0-3）
   - P0-1/P0-2 待规范侧核对（P0-1 手动验收 regsvr32/CoCreateInstance 待真实环境；P0-2 反馈 3 点待规范侧澄清）
2. clippy 集中收尾（冻结中，待功能骨架定型）
   - 补 SAFETY 注释（COM/FFI 几十处 unsafe）
   - 修单向 lint（loop/Default/let/redundant closure/transmute 等）
3. pipeline 核心模块完成度（context/buffer/format/chain/process/interleave——已确认签名可用，待统一验证）
4. object 子模块剩余（child/factory/ref_count/dll_exports 待对照 v6.4 规范核实）
5. telemetry 模块完成度
6. config/watcher.rs → object/apo.rs::hot_reload 的接线（watcher 触发链路待确认；P0-4 范畴）

## 已解决疑问
- DSP 滤波器 process() 完成度验证完成：10 个全部真实实现；VST 预留 NoMatch
- apo.rs 骨架 4 处占位全部补齐
- v6.6-v6.9 changelog 已通读并适配（O1/E1/R1-R4/O2/O3/O4/E3.2-E3.4）
- **parser 命令分发链路（P0-2 解决）**：命令关键字静态分发（条件分支始终处理 → 纯配置命令
  handle_* → DSP 命令 handle_* → REW `Filter N:` → registry.try_create 兜底裸 DSP 命令）；
  `FilterFactory::create_filter` 只收冒号后 value，config 命令工厂无法从 value 反推命令名，
  故 ConfigParser 分发完全静态化，registry 只承载 9 个 DSP 工厂
- **config/commands/*.rs handle 是否被 parser 调用（P0-2 解决）**：已全部接线——
  Device/Stage/Channel/Eval/Include/Filter/GraphicEQ/Preamp/Copy/Delay 静态分发 + REW 动态命令名
- **6 个未注册的 index 0-5 工厂（P0-2 解决）**：index 0-5（DEVICE/IF/EVAL/INCLUDE/STAGE/CHANNEL）
  是纯配置命令，由 parser 静态分发，不需要在 FilterRegistry 占位（无工厂可匹配其 value 形态）；
  REW 的 RewFactory（6.3 列出）同样无法从 value 反推 `Filter N:` 命令名，parser 直接分发 rew::handle

## 未解疑问清单
- install/audiodg.rs 与 selector/operation.rs 的 DisableProtectedAudioDG 检查时机（ensure_can_load 已在 LockForProcess 末尾调用）
- config/watcher.rs 检测到配置变化后如何触发 apo.rs::hot_reload（需要 watcher → 持有 ApoObject 的关联；当前 hot_reload 是 pub 方法待接线；P0-4 范畴）
- P0-2 反馈 3 点待规范侧澄清：① 6.3 vs 6.1 矛盾（config 命令工厂注册意图）② 6.1 current_file 原型（&'a Path → PathBuf）③ 6.15 REW 动态命令名未在 6.1 分发描述