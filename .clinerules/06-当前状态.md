# 当前状态（截至 commit a325ad3）

## 已完成
- Git 仓库初始化 + 备份到 vxapo-driver-bak
- 旧代码清理 + checkpoint
- 目标树全声明补全 + 参与编译
- config 12 commands 补齐
- GUID 封装（StringFromGUID2 in prelude）
- SAM_READ 修复（0x0002_0019）
- DSP 工厂 15 槽位全接线（9 个 DSP 工厂实现，index 0-5 为纯配置命令）
- **v6.4 适配（commit cba65fd）**：
  - install/selector 拆分为 select.rs（设备选择交互）+ operation.rs（安装/卸载/事务回滚），selector.rs 为纯入口
  - install/device/info.rs enumerate_devices 真实枚举（Render + Capture，经 enum_sub_keys）
  - sys/registry.rs 补齐枚举/备份 API + get_guid_string + save_to_file（.reg 导出）
  - 清理 slots.rs / operation.rs 多余 GUID 转发封装
- **DSP 滤波器完成度（commit 7be0aab）**：
  - convolution.rs 短 IR 时域 FIR 卷积 + WAV 解析（PCM16/f32）+ 环形延迟线（RT 安全）
  - VST 定位为预留 NoMatch；FACTORY_COUNT=15 / VST_PLUGIN index=13 不变；删除 libloading + vst feature
  - 10 个滤波器 process() 全部真实实现，无空壳
- **object/apo.rs 骨架补全（commit a325ad3）**：
  - LockForProcess 完整链路：状态机 → pFormat(ManuallyDrop<Option<IAudioMediaType>>) → extract_format → PipelineContext+DspContext → ConfigParser.parse_file → Chain + 预分配 temp_buffers → latency 原子写入 → ensure_can_load → RAII LockGuard 回退
  - APOProcess 双链过渡：正常模式走 process_audio；过渡模式双 process_chain_interleaved + SmoothingProvider.advance 升余弦混合 + 完成时清 outgoing/pending_reload 触发 hot_reload
  - GetRegistrationProperties：vx_reg_props（PRE_MIX/POST_MIX）按 CLSID 选择 + CoTaskMemAlloc 拷贝
  - IsInput/OutputFormatSupported：extract_format 对比 PipelineContext（通道数/采样率）
  - hot_reload：锁外解析新链 + 锁内 SmoothingProvider 过渡
- cargo check 0 errors / 0 warnings
- cargo test 417 passed / 0 failed

## 进行中 / 待做
1. clippy 集中收尾（冻结中，待功能骨架定型）
   - 补 SAFETY 注释（COM/FFI 几十处 unsafe）
   - 修单向 lint（loop/Default/let/redundant closure/transmute 等）
2. pipeline 核心模块完成度（context/buffer/format/chain/process/interleave——已确认签名可用，待统一验证）
3. object 子模块剩余（child/factory/ref_count/dll_exports 已存在，待对照 v6.4 规范核实）
4. telemetry 模块完成度
5. config/watcher.rs → object/apo.rs::hot_reload 的接线（watcher 触发链路待确认）

## 已解决疑问
- DSP 滤波器 process() 完成度验证完成：10 个全部真实实现；VST 预留 NoMatch
- apo.rs 骨架 4 处占位（LockForProcess/APOProcess/GetRegistrationProperties/IsFormatSupported）全部补齐

## 未解疑问清单
- parser 的命令分发链路：命令关键字 vs DSP 类型关键字在 parse_lines_impl 中的具体区分机制
- install/audiodg.rs 与 selector/operation.rs 的 DisableProtectedAudioDG 检查时机（ensure_can_load 已在 LockForProcess 末尾调用）
- config/commands/*.rs 的 handle 函数目前是否被 parser 实际调用（还是只通过 registry.try_create）
- 6 个未注册的 index 0-5 工厂（纯配置命令）是否需要在 FilterRegistry 中占位
- config/watcher.rs 检测到配置变化后如何触发 apo.rs::hot_reload（需要 watcher → 持有 ApoObject 的关联；当前 hot_reload 是 pub 方法待接线）
- utils/guid 冲突已澄清：GUID→String 统一走 prelude；bytes→GUID 就地内联