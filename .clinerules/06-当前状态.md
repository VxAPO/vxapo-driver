# 当前状态（截至 commit 4a0ada2，P0-7 install/卸载/快照/EAPO 对齐闭环）

## P0-7 install/卸载/快照/EAPO 对齐链（2026-08-04 下午，driver 33f1ccf→4a0ada2，cli 419c0c3）
- **0x80070005 根因链**：MMDevices 端点键 ACL 只给管理员 SetValue/ReadKey（无 CreateSubKey）→ `RegKey::create(SAM_ALL)` 超权限被拒 → 新增 `open_for_write`（KEY_SET_VALUE|KEY_QUERY_VALUE）最小权限（33f1ccf→2075279）
- **子 APO 路径对齐**：运行期 `object/child.rs` 读 `HKLM\SOFTWARE\VxAPO\Child APOs\{guid}`，安装原写 `FxProperties\ChildApoKeys` 死代码（无人读 + ACL 拒建子键）→ 已对齐 + 无条件备份槽位名+原值（PreMixSlot/PostMixSlot/Value，2075279+0ad1035）
- **快照恢复=EAPO 恢复**：install 无条件备份被覆盖槽位原值 → uninstall 只删 VxAPO CLSID、删空才写回备份（接管者不覆盖）、恢复后删信息区——用户实测「不留子 APO 也能恢复 EAPO」（0ad1035+cec0a67）
- **槽位 REG_SZ 类型**：写 GUID 字符串（EAPO 生态 RegistryHelper.h 期望），修 wrong type 致 EAPO 无法枚举（2d33b9b）
- **子 APO 保留双 bug 根修**：删 operation.rs 自带 `read_slot_safe`（data4 12 字符传 4 字符函数 + hex_to_u32 8 字符传 4 字符函数 → 双 None）统一 `slots::read_slot_value`（bcdecc9）
- **EAPO 槽位对齐**：capture 特例只装 PreMix（C48）+ 互斥删槽位 SfxEfx 保 MFX/SfxMfx 保 EFX（C45）+ 删死代码 guid_from_bytes/hex 辅助（898e7c2）
- **三档自动探测 API**：`slots::detect_install_mode`（C41-C44 纯逻辑 + 6 测试，be196eb）+ `info::detect_mode_for_device/guid`（0c0677a+4a0ada2）；CLI `install` 缺省 `--mode` 自动探测、显式覆盖（cli 5767d04）；交互菜单 4 项改进：设备 q 回模式/h 帮助/exit→q/默认 .\config.txt（cli 419c0c3）
- verify 自检前 `CoInitializeEx`（0x800401F0）+ self-preserve 过滤（不把 VxAPO 自占当子 APO）+ 采集端点 childPostMix 置空
- cargo test **439 passed / 0 failed**，0 warnings
- 遗留：CLI 未实现 EAPO 模式三档探测的「已装保留模式」展示（C49）；EAPO E3.4 自检实际是 IAudioClient 管线自检（C37，规范 S2 需修订）

## P0-7 枚举修复最终（2026-08-04，commit 955c087，cli a5685b3）
- **Windows 真实槽位 PID 修正**（reg query 实证）：`{d04e05a6-...}` 各槽位 PID 为 **0/3/5/6/7**（非 0-4）——LFX=0 / GFX=3 / SFX=5 / MFX=6 / EFX=7；`ApoSlot::registry_pid()` 映射 + 新测试
- **槽位值类型兼容 REG_SZ**（EAPO 等第三方写 GUID 字符串）：`read_slot_value` + `operation::read_slot_safe` 自动识别 REG_SZ/Binary
- **parse_guid_string data4 越界 bug**（既有，EAPO REG_SZ 触发 panic）：每 2 hex 字符 = 1 字节
- **全零 GUID 归一 NoValue**（Windows「无 APO」占位 {00000000-...}）：否则 is_guid() 误判槽位被占
- **VxAPO 安装模式判定改 CLSID 配对**：PreMix=VXAPO_PRE **且** PostMix=VXAPO_POST 才算该模式；EAPO/系统 APO 占槽不算（[0] EDIFIER SFX/EFX 被 EAPO 占 → 正确显示 SfxEfx=未装 VxAPO）
- **设备名组合**：接口名（扬声器/耳机）+ 产品名（EDIFIER M16+/DOVE Hi-Fi DSP 等）→「扬声器 (EDIFIER M16+)」
- **CLI EAPO 状态检测**（commands.rs detect_eapo_status）：list 打印「▶ EAPO 已安装：PreMix=(SFX) + PostMix=(EFX)」
- 实测：8 设备全部显示具体名称 + 槽位 + EAPO 状态；driver 433 passed / 0 failed

## P0-7 CLI 端到端验证（2026-08-04，vxapo-cli 仓库 + 本次 driver 修复）
- vxapo-cli：依赖接入（vxapo-driver as library + windows-rs）+ status/list + install/uninstall + config set/show + snapshot（快照=变更对比+基线保持）+ 槽位失守检测 + resolve_device（GUID/序号→三元组）+ require_admin；main 重写为双模式（无参数→交互终端菜单，release exe 双击有内容；带参数→子命令自动化）
- **端点枚举 bug 修复（本次，driver P0-2 遗留）**：
  - `query_endpoint` 原读端点键顶层 `"Device"`/`"FriendlyName"`（实际 MMDevices 顶层仅 DeviceState）→ 全部返回 Ok(None) → 44 设备全「未命名」。改为读 Properties 子键 PKEY 值名（DeviceInstanceId={b3f8fa53...},2；接口友好名={a45c254e...},2；回退产品名={b3f8fa53...},6）
  - GUID 仍大多为空：Properties 的 `{9D631510-92A8-4a79-A79E-A83812C9C119},2`（PKEY_AudioEndpoint_GUID）并非所有端点都有。修复：enumerate_devices 循环内子键名为空时用 MMDevices 子键名回填 endpoint_guid（子键名即端点 GUID）
- 验证：cargo run -- list 44 设备全部显示名称 + GUID；driver cargo test 431 passed / 0 failed
- roadmap P0-7 已追加实现完成报告（2026-08-04 执行端），待规范侧合规核对

## P0-5/P0-6 完成（2026-08-04，commit dfd0b2e + e2fb954）
- P0-5 RT 入口 panic 防护：apo_process_inner + catch_unwind + Calc 保守值；新增 2 测试
- P0-6 子 APO 委托：slots.rs childApo API + child.rs 类型化委托（v8.6 Option 参数）+ apo.rs 六处接线
- 431 passed / 0 failed；roadmap 实现完成报告已追加，待规范侧合规核对

## 已完成
- Git 仓库初始化 + 备份到 vxapo-driver-bak
- 旧代码清理 + checkpoint
- 目标树全声明补全 + 参与编译
- config 12 commands 补齐
- GUID 封装（StringFromGUID2 in prelude）
- SAM_READ 修复（0x0002_0019）
- DSP 工厂 15 槽位全接线（9 个 DSP 工厂实现，index 0-5 为纯配置命令）
- **v6.4 适配（commit cba65fd）**：install/selector 拆分 + 真实枚举 + sys/registry 补齐
- **DSP 滤波器完成度（commit 7be0aab）**：10 个滤波器 process() 全部真实实现
- **object/apo.rs 骨架补全（commit a325ad3）**：LockForProcess 完整链路 + 双链过渡 + hot_reload
- **v6.9 适配（commit 0589669）**：O1/O2/O3/O4/E1/E3.2-E3.4/R1-R4 全部落地
- **P0-1 DllRegisterServer 补全（commit a3890c7，Done @ v7.4）**
- **P0-2 config.txt 解析链路补齐（commit 11a381f + 4fc6e8f，Done @ v7.4）**
- **CLSID 定稿（commit 58d2026）**：PRE_MIX=41C34613-D391-459D-A039-72B2B15A1A1D / POST_MIX=B4A97313-ABC0-45ED-9C33-428B20D39428 + 07-VxAPO_GUID.md 备份
- **P0-3 per-device 配置路径（commit bb708b3，Done @ v7.6）**：
  - sys/known_folder.rs（documents_folder + CoTaskMemGuard RAII）
  - apo_types re-export APOInitSystemEffects/PKEY_AudioEndpoint_GUID/IPropertyStore/PROPVARIANT
  - apo.rs config_path/extract_endpoint_guid/resolve_config_path/Initialize 重写 + APOProcess 过渡 3 修正
- **v7.7 时序修复（commit e26e120）**：IsInputFormatSupported 独立属性检查（禁依赖 pipeline_context）
- **P0-4 配置热重载全链路（commit ad6f203 + 7b20619 + 77bc68e）**：
  - parser.rs：FilterSpec/SpecChain/MAX_CONFIG_FILE_SIZE + produce_spec + parse_file_with_spec/parse_content_with_spec 双返回 + **v7.11/v7.12 语法严格化**（split_command_value 单冒号校验缺/多余冒号 SyntaxError + Unmatched→SyntaxError + 裸命令反转 + Convolution Result 化 + **白名单三段式** is_known_dsp_command「未知命令」不落 registry + VSTPlugin 特判「未启用（预留）」+ Unmatched 收窄「命令无效 'X'：参数无法解析」）
  - include.rs：specs 共享 chain 内联 + 128KB 子文件闸门（Include 失败=整体失败）
  - watcher.rs 事件驱动（DirectoryChanged + wait_and_handle + shutdown + poll_registry + **unsafe impl Send**）
  - apo.rs：ApoObjectInner.active_spec + hot_reload v7.9 六步 + LockForProcess 过渡缓冲预分配 + **v7.10 对象层接线（commit 77bc68e）**：Arc<Mutex<WatcherState>>（#[implement] 无 &mut Foo，方案 A）+ Lock 末尾 start_watcher（CreateEventW → ConfigWatcher::new → spawn 循环 wait_and_handle → hot_reload_impl）+ Unlock stop_watcher（SetEvent → join → CloseHandle）+ hot_reload_impl 模块级函数（线程 clone Arc，无需持有 self）
  - 测试 428 → 436 passed（parser/watcher/卷积各新增）
  - roadmap P0-4 已追加"实现完成报告"+ "反馈"段（v8.0 已外置 feedback.md #P0-4-0/-1/-2/-3/-4）
- **规则修订（commit fe1c20d）**：00-核心约束「反馈闭环」澄清——roadmap「禁止写入」限缩为仅禁反馈/修订正文，明确执行端可追加实现完成报告 + 勾选执行端待办；SKILL Step5/6 同步（Step5 完成报告必修 + 待办标注，Step6 反馈边界）
- **roadmap P0-4 实现完成报告已追加**（规范仓库）：待办 ①（接线 77bc68e）②③（严格化 7b20619）标注完成，④ 听感验证预留待 P0-7 CLI

## 进行中 / 待做
1. P0 系列：
   - **P0-4 配置热重载（所有执行端待办已闭合）**：watcher 线程接线 commit 77bc68e（roadmap 待办 ①）+ v7.11/v7.12 语法严格化 commit 7b20619（待办 ②③）——剩余：真实音频引擎听感验证（目录变更→无爆音）留手动验收
   - **P0-5 RT 入口 panic 防护（已闭合）** / **P0-6 子 APO 委托（已闭合）**
   - **P0-7 CLI 端到端（实现完成 + install/卸载/快照/子APO/EAPO 对齐已闭环）**：剩余——（a）真实音频引擎听感验证；（b）EAPO 三档探测「已装保留模式」展示（C49，CLI 未做）；（c）规范侧合规核对 + 手动 install/uninstall 真实设备验证
2. **规范对齐遗留**：EAPO E3.4 自检描述不符（实际 IAudioClient 管线自检，非 CoCreateInstance DLL 验证）——行为文档 S2，需规范修订；「协商期锁死 in==out」表述失准（S1）；child 失败销毁语义（S3）；GetLatency 无 child 返回 0（S4）；无冒号行语义有意差异需标注（S5）
3. clippy 集中收尾（冻结中）
4. pipeline 核心模块完成度验证
5. object 子模块剩余（child 委托 P1-5）
6. telemetry 模块完成度

## 已解决疑问
- DSP 滤波器 process() 完成度：10 个全部真实实现；VST 预留 NoMatch
- parser 命令分发链路（P0-2）：静态分发 + registry 兜底
- 6 个未注册的 index 0-5 工厂：纯配置命令由 parser 静态分发
- P0-2 反馈 3 点 → v7.4 采纳；P0-3 反馈 2 点 → v7.6 采纳
- v7.7 时序缺陷：IsInputFormatSupported 早于 LockForProcess → 独立属性检查
- **v8.0 反馈外置**：规范问题统一记 feedback.md（PX-X 格式），roadmap 只留引用；规则已修订（00 反馈闭环段 + implement-task skill Step5/6）
- **0x80070005 超权限根因**：MMDevices 端点键管理员仅 SetValue/ReadKey，SAM_ALL 含 CreateSubKey 位必被拒 → open_for_write 最小权限（已解决 2075279）
- **子 APO 读不到根因**：operation.rs 自带解析双 bug（data4 12 字符 + data1 8 字符都传给要求 4 字符的 hex_to_bytes → 双 None）→ 删死代码统一 read_slot_value（已解决 bcdecc9）
- **快照恢复变 NoValue 根因**：install 覆盖槽位不备份原值 + uninstall 只删不恢复 → 无条件备份槽位原值 + 删空才写回（已解决 0ad1035）

## 未解疑问清单
- install/audiodg.rs 与 selector/operation.rs 的 DisableProtectedAudioDG 检查时机
- ~~P0-4 watcher 线程接线~~（已解决：Arc<Mutex> 方案 A + start/stop_watcher）
- P0-4 真实 Windows 音频引擎验证（目录变更→热重载无爆音）需真实 audiodg 环境
- EAPO 三档探测「已装保留模式」展示（C49）：CLI list 目前按 VxAPO CLSID 配对判定模式，未做 foundAt 槽位保留推导——待实现/待规范确认