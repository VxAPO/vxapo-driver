# 当前状态（截至 commit ad6f203）

## 已完成
- Git 仓库初始化 + 备份到 vxapo-driver-bak
- 旧代码清理 + checkpoint
- 目标树全声明补全 + 参与编译
- config 12 commands 补齐
- GUID 封装（StringFromGUID2 in prelude）
- SAM_READ 修复（0x0002_0019）
- DSP 工厂 15 槽位全接线（9 个 DSP 工厂实现，index 0-5 为纯配置命令）
- **v6.4 适配（commit cba65fd）**：install/selector 拆分 + 真实枚举 + sys/registry 补齐
- **DSP 滤波器完成度（commit 7be0aab）**：10 个滤波器 process() 全部真实实现
- **object/apo.rs 骨架补全（commit a325ad3）**：LockForProcess 完整链路 + 双链过渡 + hot_reload
- **v6.9 适配（commit 0589669）**：O1/O2/O3/O4/E1/E3.2-E3.4/R1-R4 全部落地
- **P0-1 DllRegisterServer 补全（commit a3890c7，Done @ v7.4）**
- **P0-2 config.txt 解析链路补齐（commit 11a381f + 4fc6e8f，Done @ v7.4）**
- **CLSID 定稿（commit 58d2026）**：PRE_MIX=41C34613-D391-459D-A039-72B2B15A1A1D / POST_MIX=B4A97313-ABC0-45ED-9C33-428B20D39428 + 07-VxAPO_GUID.md 备份
- **P0-3 per-device 配置路径（commit bb708b3，Done @ v7.6）**：
  - sys/known_folder.rs（documents_folder + CoTaskMemGuard RAII）
  - apo_types re-export APOInitSystemEffects/PKEY_AudioEndpoint_GUID/IPropertyStore/PROPVARIANT
  - apo.rs config_path/extract_endpoint_guid/resolve_config_path/Initialize 重写 + APOProcess 过渡 3 修正
- **v7.7 时序修复（commit e26e120）**：IsInputFormatSupported 独立属性检查（禁依赖 pipeline_context）
- **P0-4 配置热重载全链路（commit ad6f203，v7.9 实现+测试）**：
  - parser.rs：FilterSpec/SpecChain/MAX_CONFIG_FILE_SIZE + produce_spec（命令名小写+\x1F+token 规范化）+ parse_file_with_spec/parse_content_with_spec 双返回 + 裸命令可达性（value 空→try_create(cmd)）
  - include.rs：specs 共享 chain 内联 + 128KB 子文件闸门（Include 失败=整体失败）
  - watcher.rs 重写（事件驱动）：WatchEvent::DirectoryChanged + new(watch_dir, shutdown_event) + wait_and_handle（WaitForMultipleObjects + 10ms 去重）+ shutdown + poll_registry + Drop 兜底
  - apo.rs：ApoObjectInner.active_spec（Lock 基线/hot_reload 更新/Unlock·Reset 清空）+ hot_reload v7.9 六步（R2 阻塞→128KB 闸门→锁外 parse_file_with_spec→spec 短路→锁内交换+active_spec 更新）+ LockForProcess 过渡缓冲预分配（max_frame_count×max_ch）
  - 测试 428 → 434 passed（新增 6：produce_spec 格式/复杂 config 含 Include/乱码 lossy/超限子文件 等）
  - roadmap P0-4 已追加"实现完成报告"+ "反馈"段（watcher 线程接线遗留 + config 6.2 自启矛盾），未动状态
- cargo check 0 errors / 0 warnings
- cargo test 434 passed / 0 failed

## 进行中 / 待做
1. P0 系列：
   - P0-4 配置热重载（**实现完成 commit ad6f203**，待规范侧核对 + **watcher 线程接线遗留**：ApoObject 需在 LockForProcess 末尾自建线程循环 wait_and_handle→hot_reload + Unlock 时 SetEvent+join；当前 ConfigWatcher 仅单元测试覆盖语义）← **当前**
   - P0-5 RT 入口 panic 防护（Backlog）
2. clippy 集中收尾（冻结中）
3. pipeline 核心模块完成度验证
4. object 子模块剩余（child 委托 P1-5）
5. telemetry 模块完成度
6. P1-5 子 APO 委托（child.rs 落地）

## 已解决疑问
- DSP 滤波器 process() 完成度：10 个全部真实实现；VST 预留 NoMatch
- parser 命令分发链路（P0-2）：静态分发 + registry 兜底
- 6 个未注册的 index 0-5 工厂：纯配置命令由 parser 静态分发
- P0-2 反馈 3 点 → v7.4 采纳；P0-3 反馈 2 点 → v7.6 采纳
- v7.7 时序缺陷：IsInputFormatSupported 早于 LockForProcess → 独立属性检查

## 未解疑问清单
- install/audiodg.rs 与 selector/operation.rs 的 DisableProtectedAudioDG 检查时机
- **P0-4 watcher 线程接线**：ApoObject 生命周期/COM 边界下如何持有 shutdown_event + 自建线程（ConfigWatcher 外部驱动 vs 自启矛盾待规范侧统一）
- P0-4 真实 Windows 音频引擎验证（目录变更→热重载无爆音）需真实 audiodg 环境