# 当前状态（截至 commit 7be0aab）

## 已完成
- Git 仓库初始化 + 备份到 vxapo-driver-bak
- 旧代码清理 + checkpoint
- 目标树全声明补全 + 参与编译
- config 12 commands 补齐
- GUID 封装（StringFromGUID2 in prelude）
- SAM_READ 修复（0x0002_0019）
- DSP 工厂 15 槽位全接线（9 个 DSP 工厂实现，index 0-5 为纯配置命令）
- **v6.4 适配（commit cba65fd）**：
  - install/selector 拆分为 select.rs（设备选择交互）+ operation.rs（安装/卸载/事务回滚），selector.rs 为纯入口
  - install/device/info.rs enumerate_devices 真实枚举（Render + Capture，经 enum_sub_keys）
  - sys/registry.rs 补齐枚举/备份 API：enum_sub_keys / enum_values / key_exists_child / read_multi_value / get_guid_string / key_exists / value_exists / delete_tree / is_windows_version_at_least / save_to_file（含 dump_key_recursive .reg 导出）
  - 安装备份升级为真实 .reg 导出（save_to_file）
  - 清理 slots.rs / operation.rs 多余 GUID 转发封装，统一 use prelude::guid_to_string
  - 新增 REG_SAM_FLAGS import（cargo check 修复）
- **DSP 滤波器完成度（commit 7be0aab）**：
  - **convolution.rs 真实实现**：短 IR（<256 采样）时域 FIR 卷积 + WAV 解析（PCM16/f32）+ 增益合并 + latency 如实报告 + 环形延迟线（RT 安全无分配无锁）；长 IR 跳过并 warn
  - **VST 定位为预留**：vst.rs 仅保留注释（给开发者看）；VstFactory 恒返回 NoMatch；FACTORY_COUNT=15 / VST_PLUGIN index=13 不变；删除 libloading + vst feature
  - 验证结论：biquad/peq/hp_lp/loudness/graphic_eq/copy/gain/delay/transition/convolution 共 10 个滤波器 process() 全部真实实现，无空壳
- cargo check 0 errors / 0 warnings
- cargo test 417 passed / 0 failed（convolution +10 测试，vst 测试 -2，净 +8）

## 进行中 / 待做
1. clippy 集中收尾（冻结中，待功能骨架定型）
   - 补 SAFETY 注释（COM/FFI 几十处 unsafe）
   - 修单向 lint（loop/Default/let/redundant closure/transmute 等）
2. pipeline 核心模块完成度（context/buffer/format/chain/process/interleave）
3. object 子模块完成度（child/factory/ref_count/dll_exports + apo.rs StateCell）
4. telemetry 模块完成度

## 已解决疑问
- DSP 滤波器 process() 完成度验证完成：9 个原有真实实现 + convolution 补真实实现；VST 确认不需要（评估后定为预留 NoMatch）

## 未解疑问清单
- parser 的命令分发链路：命令关键字 vs DSP 类型关键字在 parse_lines_impl 中的具体区分机制
- install/audiodg.rs 与 selector/operation.rs 的 DisableProtectedAudioDG 检查时机（ensure_can_load 在 LockForProcess 调用链）
- config/commands/*.rs 的 handle 函数目前是否被 parser 实际调用（还是只通过 registry.try_create）
- 6 个未注册的 index 0-5 工厂（纯配置命令）是否需要在 FilterRegistry 中占位
- utils/guid 冲突已澄清：GUID→String 统一走 prelude::guid_to_string；bytes→GUID 就地内联，不恢复 utils/guid.rs（v6.4 install 规范措辞落后于已确认架构决策）