# 已确认的架构决策（用户批准，勿再争论）

## GUID 格式化
- sys/com/prelude.rs 内：pub use StringFromGUID2 + pub fn guid_to_string
- 所有调用点统一走 prelude::guid_to_string（slots.rs / selector.rs / vx_reg_props.rs / endpoint.rs）
- guid_win crate 已被否决（依赖 winapi 而非 windows-rs）
- utils/guid.rs 已删除

## APO 类型方案（方案 A）
- apo_types.rs 中 windows-rs 已有类型全部 re-export（camelCase）
- 仅保留 windows-rs 缺失的自定义项
- #[interface] / 自创 APO trait 是错的，应 re-export windows-rs 结构体 + *_Impl trait

## DSP 工厂架构
- Filter: Send + Sync + Debug，process(&mut [Vec<f32>], usize)，RT-safety 无分配无锁
- FilterFactory::create_filter(&self, params, ctx:&DspContext, loader:&dyn ConfigLoader) -> FilterCreateResult
- FilterCreateResult: Filter / NoFilter / NoMatch / AbortFile
- FilterRegistry::try_create 遍历所有已注册工厂
- FACTORY_COUNT=15，index 常量 0-14 对应注册顺序

## IIRFactory 类型覆盖
- PK/PEAK → PeakingFilter
- LP/HP → HighLowPassFilter
- LS/HS/AP/NO → compute_coeffs + BiquadFilter（临时封装）
- Modal → NoMatch（不报错，留未来扩展）

## VST 定位（用户 2026-08-01 确认：评估后不需要）
- vst.rs 仅保留模块注释（给开发者看），不提供任何实现
- VstFactory 恒返回 NoMatch（`VSTPlugin:` 配置静默跳过）
- FACTORY_COUNT=15 / VST_PLUGIN index=13 / register_builtin_filters 注册 VstFactory 全部保留，未来加 VST2/VST3 无需调整索引结构
- Cargo.toml 删除 libloading 可选依赖 + vst feature
- ~~VstFilter~~ 已删除（之前是空壳，违反禁止空壳约束）

## Convolution 实现方案（Phase 8 选定：短 IR 时域）
- IR < 256 采样：直接时域 FIR 卷积（环形延迟线，RT 安全无分配无锁）
- latency = ir_len - 1 如实报告；增益线性合并进 IR 系数
- WAV 解析：PCM16 / f32，单声道 IR 复制多通道，1 MiB 大小上限
- 长 IR（≥256）或加载失败：跳过该滤波器 + log warn（不报错）
- 长 IR 的分段卷积（Partitioned）留待未来

## config/parser 命令分发
- parse_lines_impl 中 registry.try_create(value) 分发 DSP 命令
- 纯配置命令（Device/If/Stage/Channel/Eval/Include）经命令处理器
- NullConfigLoader 为 pub(crate)

## v6.6-v6.9 适配决策（2026-08-01，tympan-apo/EAPO 借鉴）
- O1 RT 编译期见证：RealtimeContext（零尺寸，contract.rs）+ DspContext::rt_marker（PhantomData）
- O2 StateCell 补全：TransitionError{expected,attempted,actual} + release()（任意态→Created）+ initialize/lock/unlock 语义化
- O3 生产构建约束：release `panic="abort"` + `codegen-units=1`（RT 跨 FFI unwind = UB）
- O4 AEC 接口预留：`feature="aec"` 门控 3 接口 + IID（IApoAcousticEchoCancellation/AuxiliaryInputConfiguration/AuxiliaryInputRT）
- E1 就地处理声明：Filter::is_in_place()（默认 true）+ Chain::is_fully_in_place()
- E3.2 设备物理状态谓词：DeviceInfo::is_disabled()/is_unplugged()（EndpointState 推导）
- E3.3 InstallConfig::auto_adjust 独立字段（默认 false），Step4 写注册表读取
- E3.4 install_endpoint(..., verify)：commit 后 CoCreateInstance 验证，失败不自动回滚
- R1 退役链延迟析构：RT 过渡完成帧仅 `retired_chain = outgoing_chain.take()`（零析构），控制线程锁内统一 drop
- R2 阻塞式重载：hot_reload 检查 `transition.is_some() || reloading` 即返回；过渡完成由 APOProcess 触发
- R3 空链快路径：Chain::is_empty() 时 process_audio 直接跳过链遍历（temp_buffers 即输出）
- R4 过渡周期 10ms：default_smoothing_length 由 sample_rate/20 → sample_rate/100

## clippy 策略（方案 2）
- 功能骨架优先，clippy 集中收尾
- lib.rs 的 #![deny(clippy::undocumented_unsafe_blocks)] 造成的 SAFETY 注释需求暂冻结
- 待功能定型后统一处理

## audiodg 测试
- 使用独立子键 test_path(name) 避免并行竞态
- 用 eprintln 调试定位（用户坚持，不要瞎猜）
